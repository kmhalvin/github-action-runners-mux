# Maximum concurrent ephemeral worker containers
max_workers: 5

# Number of pre-booted warm containers for zero-latency job starts
warm_workers: 2

runners:
  # Repository-level runner
  - name: "Runner 1"
    url: "https://github.com/my-org/repo-A"
    pat: "ghp_xxxxxxxxxxxx"
    scale_set_name: "repo-a-runners"
    max_runners: 5
    labels:
      - "ubuntu-latest"
      - "x64"

  # Organization-level runner
  - name: "Runner 2"
    url: "https://github.com/my-org"
    pat: "ghp_yyyyyyyyyyyy"
    scale_set_name: "org-runners"
    group: "Default"
    labels:
      - "self-hosted"
      - "gpu"
    # max_runners omitted; will fall back to global max_workers (5)

  # Enterprise-level runner with a custom runner group
  - name: enterprise-shared
    url: https://github.com/enterprises/my-enterprise
    pat: ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
    scale_set_name: enterprise-runners
    group: production
